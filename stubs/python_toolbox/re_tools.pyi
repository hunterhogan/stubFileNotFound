from typing import Any

def searchall(pattern: Any, string: Any, flags: int = 0) -> Any:
    """
    Return all the substrings of `string` that match `pattern`.

    Note: Currently returns only non-overlapping matches.
    """



